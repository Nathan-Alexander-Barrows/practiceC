#include <stdio.h>

/**
 * Description
 */
void main(void)
{
	/**
	 * IF statment: the 'if' statment is used to execute
	 * a block of code IF a specified condition is true.
	 */
	if (condition)
	{
		/**
		 * code to be executed "if" the "condition" is true
		 */

		/**
		 * ELSE statment: the 'else' statment can be used to execute
		 * a block of code "if" the "condition" is false
		 */
		if (condition)
		{
			/**
			 * sode to be executed "if" the "condition" is true
			 */
		}
		else
		{
			/**
			 * code to execute if the "conditoon" is false
			 */
		}

		/**
		 * ELSE IF statment: you can also use the "else if" statment
		 * to check multiple conditions
		 */
	       if (condition1)
	       {
	/**
	 * code to execute if "conditon1" is true
	 */
	       }
	       else if (condition2)
	       {
	/**
	 * code to execute if "conditon2" is true
	 */
	       }
	       else
	       {
	/**
	 * code to execute if both "condition1" and "condition2" are false
	 */
	       }

